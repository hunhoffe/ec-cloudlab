
whisk:
  ingress:
    type: NodePort
    apiHostName: 192.168.6.10
    apiHostPort: 31001
  runtimes: "runtimes-minimal-travis.json"
k8s:
  persistence:
    enabled: false
nginx:
  httpsNodePort: 31001
invoker:
  containerFactory:
    impl: "kubernetes"
    kubernetes:
      replicaCount: NUM_INVOKERS
